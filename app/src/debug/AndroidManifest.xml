<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application>
        <!-- Debug-only LLM prompt harness; see LlmProbeReceiver. DUMP is held by
             adb shell but not grantable to third-party apps. -->
        <receiver
            android:name="com.pennywiseai.tracker.debug.LlmProbeReceiver"
            android:exported="true"
            android:permission="android.permission.DUMP" />
    </application>
</manifest>
